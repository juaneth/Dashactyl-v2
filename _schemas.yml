user:
  username: string
  email: string
  password: string
  coins: number
  package: string
  resources:
    ram: string-number
    disk: string-number
    cpu: string-number
    servers: string-number
  created_at: number

package:
  name: string
  is_default: boolean
  ram: string-number
  disk: string-number
  cpu: string-number
  servers: string-number
